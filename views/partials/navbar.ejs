<div class="container">
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img src="logo.png" alt="" width="60" height="50">Paws n' Whiskers
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a id="home-link" class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a id="services-link" class="nav-link" href="/services">Services</a>
          </li>
          <li class="nav-item">
            <a id="news-link" class="nav-link" href="/news">News</a>
          </li>
          <li class="nav-item">
            <a id="contact-link" class="nav-link" href="/contact">Contact</a>
          </li>
          <li class="nav-item">
            <a id="login-link" class="nav-link" href="/login">Login/Register</a>
          </li>
          <li class="nav-item" id="logout-item" style="display: none;">
            <a id="logout-link" class="nav-link" href="/logout">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</div>

<style>
  .navbar {
    background-color: #ffffff;
    z-index: 1;
    width: 100%;
  }
  .navbar-brand {
    color: #48C5B5;
  }
  .nav-link:hover {
    color: #48C5B5;
  }
  .nav-link.active {
    color: #48C5B5;
    font-weight: bold;
  }
</style>

<script>
    function setActiveLink() { //function that sets the active link color and class based on the page we are on
      const path = window.location.pathname; // get the current path
      const homeLink = document.querySelector('#home-link'); // get the home link from the navbar
      const servicesLink = document.querySelector('#services-link'); // get the services link from the navbar
      const newsLink = document.querySelector('#news-link'); // get the news link from the navbar
      const contactLink = document.querySelector('#contact-link'); // get the contact link from the navbar
      const loginLink = document.querySelector('#login-link'); // get the login link from the navbar
    
      const activeLink = '#48C5B5'; // set the active link color to #48C5B5

      if (path === '/') { // if the path is /, set the home link to active
        homeLink.classList.add('active');
        homeLink.style.color = activeLink;
      } else if (path === '/services') { // if the path is /services, set the services link to active
        servicesLink.classList.add('active');
        servicesLink.style.color = activeLink;
      } else if (path === '/news') { // if the path is /news, set the news link to active
        newsLink.classList.add('active');
        newsLink.style.color = activeLink;
      } else if (path === '/contact') { // if the path is /contact, set the contact link to active
        contactLink.classList.add('active');
        contactLink.style.color = activeLink;
      } else if (path === '/login') { // if the path is /login, set the login link to active
        loginLink.classList.add('active');
        loginLink.style.color = activeLink;
      } else if (path === '/appointment') { // if the path is /appointment, set the login link to active
        loginLink.classList.add('active');
        loginLink.style.color = activeLink;
      } else if ([ath === '/register']) { // if the path is /register, set the login link to active
        loginLink.classList.add('active');
        loginLink.style.color = activeLink;
      }
    }
    function setActiveLink() { // function that sets the active link color and class based on the page we are on
    
    const loginItem = document.querySelector('#login-item'); // get the login item from the navbar
    const logoutItem = document.querySelector('#logout-item'); // get the logout item from the navbar

   
    const isLoggedIn = req.session && req.session.user ? true : false; // check if the user is logged in

    if (isLoggedIn) { // if the user is logged in, hide the login item and show the logout item
      loginItem.style.display = 'none'; 
      logoutItem.style.display = 'block';
    } else { // if the user is not logged in, hide the logout item and show the login item
      loginItem.style.display = 'block';
      logoutItem.style.display = 'none';
    }
  }
    setActiveLink(); // call the function on page load
    
</script>